# Required — get a free key at https://reqres.in/
REQRES_API_KEY=your_key_here

# Optional overrides (defaults shown)
# API_BASE_URL=https://reqres.in
# UI_BASE_URL=https://www.saucedemo.com
# API_USER_AGENT=ServitaTestFramework/1.0
# HEADLESS=true
# DEFAULT_TIMEOUT=30000

# Multi-environment: set TEST_ENV to load .env.<env> file
# e.g. TEST_ENV=dev loads .env.dev, then falls back to .env for missing values
# Supported: local (default), dev, uat, preprod
