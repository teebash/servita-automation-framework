# Pre-production environment — override URLs to point at preprod instances
# The REQRES_API_KEY is loaded from the base .env file as a fallback

UI_BASE_URL=https://preprod.saucedemo.com
API_BASE_URL=https://reqres.in
# HEADLESS=true
# DEFAULT_TIMEOUT=45000
