# UAT environment — override URLs to point at UAT instances
# The REQRES_API_KEY is loaded from the base .env file as a fallback

UI_BASE_URL=https://uat.saucedemo.com
API_BASE_URL=https://reqres.in
# HEADLESS=true
# DEFAULT_TIMEOUT=30000
